package com.vacanSee.controller;

import org.springframework.stereotype.Controller;

@Controller
public class FrontendController {

    // Spring Boot automatically serves index.html for "/" requests from static resources
    // No need for manual routing - just let the static resource handler do its job
}
